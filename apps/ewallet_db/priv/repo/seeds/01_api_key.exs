defmodule EWalletDB.Repo.Seeds.APIKeySeed do
  def seed do
    [
      args: [],
      args_banner: nil,
      run_banner: "Seeding an Admin Panel API key",
    ]
  end

  def run(_args) do
  end
end
